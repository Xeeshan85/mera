# PROMPT FOR HASHIM — paste this entire thing into your Antigravity

---

## Context: CIRO — Crisis Intelligence & Response Orchestrator

I'm building the **Android mobile app** for our hackathon project CIRO. The app is a **command-center dashboard** for an AI-powered urban emergency response system built for Islamabad, Pakistan.

### What's already done (by my teammates):

**Backend (Python):**
- 6 ADK agents (Signal Fusion, Crisis Detection, Severity Prediction, Resource Allocation, Stakeholder Notification, Orchestrator) that process emergency signals and manage incidents
- All agents write their output to **Firestore** (project: `mira0911`)
- Firebase Cloud Messaging (FCM) sends push notifications to topics: `public_alerts`, `emergency_services`, `admin`

**Android App (Kotlin scaffold — already generated):**
My teammate generated the complete Android project scaffold and pushed it to git. I have already pulled it. The project is in the `android/` folder of our repo.

### Project structure (already exists in android/):
```
android/
├── build.gradle.kts              ← Root: AGP 8.7.3, Kotlin 2.1.0
├── settings.gradle.kts           ← Project name "CIRO"
├── gradle.properties             ← AndroidX, parallel builds
├── gradlew / gradlew.bat
├── gradle/wrapper/gradle-wrapper.properties  ← Gradle 8.9
├── .gitignore
├── README.md
└── app/
    ├── build.gradle.kts          ← Firebase BOM 34.13, Maps Compose 8.3, Vico 3.1
    ├── proguard-rules.pro
    └── src/main/
        ├── AndroidManifest.xml   ← Permissions, Maps key placeholder, FCM service
        ├── res/                  ← strings, colors, themes, launcher icons
        └── java/com/ciro/app/
            ├── CiroApplication.kt              ← Firebase init + FCM topic subscriptions
            ├── MainActivity.kt                 ← 3-tab bottom nav (Dashboard, Map, Alerts)
            ├── data/
            │   ├── model/
            │   │   ├── Incident.kt             ← Firestore incidents schema
            │   │   ├── Resource.kt             ← Firestore resources schema  
            │   │   ├── CiroNotification.kt     ← Firestore notifications schema
            │   │   └── AgentTrace.kt           ← agent_traces + metrics schemas
            │   └── repository/
            │       └── CiroRepository.kt       ← 5 callbackFlow Firestore snapshot listeners
            ├── viewmodel/
            │   └── DashboardViewModel.kt       ← 6 StateFlows + computed stats
            ├── ui/
            │   ├── theme/CiroColors.kt         ← Dark command-center palette
            │   ├── dashboard/DashboardScreen.kt ← Severity cards, resource bar, AI trace terminal
            │   ├── map/CrisisMapScreen.kt       ← Google Maps with incident/resource markers
            │   ├── components/
            │   │   ├── SeverityForecastChart.kt ← Vico 3.1 bar chart (T+1h/T+2h/T+6h)
            │   │   └── AgentTraceTerminal.kt    ← Dark terminal showing AI reasoning logs
            │   └── notifications/
            │       └── NotificationsScreen.kt   ← Stakeholder alert feed
            └── service/
                └── CiroFirebaseMessagingService.kt ← FCM push handler
```

### What I need you to do:

## TASK 1: Set up google-services.json

My teammate already registered the Android app in Firebase Console (`com.ciro.app` on project `mira0911`) and sent me the `google-services.json` file. I need to place it at `android/app/google-services.json`. Help me verify it's in the right location and that the package name matches.

## TASK 2: Add the Google Maps API Key

The Google Maps API key for our project is the same key used by the Python backend. Check if there's a `.env` file in the repo root that contains `GOOGLE_MAPS_API_KEY`. If it exists, take that key and:
1. Create/update `android/local.properties` with `MAPS_API_KEY=<the key>`
2. The `app/build.gradle.kts` already reads this via `project.findProperty("MAPS_API_KEY")` and injects it into AndroidManifest.xml via `manifestPlaceholders`

If there's no `.env` file, tell me and I'll get the key from my teammate.

## TASK 3: Open, Sync, and Fix Build Errors

Open the `android/` folder as an Android project. When Gradle syncs:
- If `gradle-wrapper.jar` is missing, Android Studio should auto-generate it (the `gradle-wrapper.properties` already points to Gradle 8.9)
- Fix any compile errors in the Kotlin files. The codebase was generated by AI so there may be minor issues like:
  - Missing imports
  - API changes in Maps Compose 8.3 or Vico 3.1
  - Compose compiler compatibility issues
  - BottomSheetScaffold API changes in latest Material3
- **DO NOT change the architecture** — keep the MVVM pattern with CiroRepository → DashboardViewModel → Compose screens
- **DO NOT change Firestore collection names or field mappings** — these match the Python backend exactly

## TASK 4: Enable Maps SDK

In Google Cloud Console (project `mira0911`), make sure these APIs are enabled:
- **Maps SDK for Android**
- **Places API (New)** (should already be enabled from backend)

## TASK 5: Run the App

Build and run on an emulator or physical device (API 26+). The app should show:
- **Dashboard tab**: CIRO header with live indicator, 5 severity cards, resource deployment bar, pipeline stats, active incident list, severity forecast chart, AI agent trace terminal
- **Map tab**: Google Maps centred on Islamabad (33.69°N, 73.05°E) with incident markers colour-coded by severity and resource markers
- **Alerts tab**: Notification feed from stakeholder channels

All data comes from Firestore in real-time. If the database is empty, screens will show empty states — that's expected until we run the Python demo script.

## TASK 6: Fix Any Runtime Issues

Common issues to watch for:
- **Firestore permission denied**: The `firestore.rules` file allows all read/write until June 19, 2026, so this shouldn't happen
- **Map shows grey tiles**: Maps SDK not enabled in Cloud Console, or API key is wrong
- **Crash on CiroRepository**: The `whereNotIn` query on incidents requires a composite index — check Logcat for a Firestore URL to create it, or just remove the `whereNotIn` filter temporarily
- **Vico chart crash**: If Vico 3.1 API has changed, fall back to a simple Column with colored Box composables as a placeholder chart

## Important constraints:
- **Firebase project**: `mira0911`
- **Package name**: `com.ciro.app`
- **Min SDK**: 26 (Android 8.0)
- **Target SDK**: 35
- **NO `-ktx` suffix** on Firebase dependencies — they deprecated KTX modules in Firebase BOM 34+
- **Dark theme only** — this is a command-center UI, always dark
- **Firestore collections read by the app**: `incidents`, `resources`, `notifications`, `agent_traces`, `metrics`

## After it's running:
Let me know and we'll connect to test with live data from the Python backend. If you make any changes to fix build errors, commit and push them so the rest of the team has the working version.
